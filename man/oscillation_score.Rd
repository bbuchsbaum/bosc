% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oscore.R
\name{oscillation_score}
\alias{oscillation_score}
\title{Oscillation Score for binary or continuous data}
\usage{
oscillation_score(
  signal,
  fs,
  flim,
  quantlim = NULL,
  smoothach = TRUE,
  smoothwind = NULL,
  peakwind = NULL,
  thresangle = 10,
  mincycles = 3,
  minfreqbandwidth = NULL,
  fpeak = NULL,
  warnings = TRUE,
  fcor = FALSE,
  taper = c("none", "hann", "hanning"),
  signal_mode = c("auto", "event", "continuous"),
  plot = FALSE
)
}
\arguments{
\item{signal}{numeric vector (binary or continuous).}

\item{fs}{sampling rate in Hz.}

\item{flim}{length-2 numeric vector \code{c(fmin, fmax)} in Hz.}

\item{quantlim}{optional length-2 quantile bounds for trimming nonzero indices.}

\item{smoothach}{logical; smooth the ACH with a Gaussian kernel.}

\item{smoothwind}{smoothing kernel width (seconds); default matches MATLAB heuristic.}

\item{peakwind}{slow-kernel width for peak removal (seconds); default matches MATLAB heuristic.}

\item{thresangle}{angular threshold for peak detection (degrees).}

\item{mincycles}{minimum cycles to include at \code{fmin}.}

\item{minfreqbandwidth}{optional minimum bandwidth; returns NA if unmet.}

\item{fpeak}{optional frequency of interest; overrides peak search.}

\item{warnings}{logical; emit warnings.}

\item{fcor}{logical; apply 1/f correction in spectral peak estimation.}

\item{taper}{taper applied before FFT; one of \code{"none"}, \code{"hann"}, \code{"hanning"}.}

\item{signal_mode}{one of \code{"auto"}, \code{"event"}, or \code{"continuous"}.
\code{"event"} uses event-count heuristics for effective frequency bounds;
\code{"continuous"} uses signal-variance heuristics and Nyquist-limited bounds.}

\item{plot}{logical; if TRUE, plots intermediate spectra (optional, requires ggplot2).}
}
\value{
list with \code{oscore}, \code{fosc}, \code{flim}, \code{flimfft}, \code{freqs}.
}
\description{
Port of the MATLAB \code{oscillationScore} algorithm with robustness fixes.
Computes an autocorrelogram, removes the central peak, finds the spectral peak,
and returns the oscillation score.
}
