% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circular_stats.R
\name{circ_mean}
\alias{circ_mean}
\title{Circular Mean Direction}
\usage{
circ_mean(alpha, w = NULL, dim = 1)
}
\arguments{
\item{alpha}{numeric vector or matrix of angles in radians.}

\item{w}{optional weights (same dimensions as \code{alpha}). If NULL, uniform weights.}

\item{dim}{integer; dimension along which to compute mean (default 1).}
}
\value{
If \code{alpha} is a vector, returns a single mean direction. If matrix,
returns a vector of means computed along \code{dim}.
}
\description{
Computes the mean direction for circular data. Port of MATLAB \code{circ_mean}
from the Circular Statistics Toolbox (Berens, 2009).
}
\examples{
angles <- c(0, pi/4, pi/2)
circ_mean(angles)

# Weighted mean
circ_mean(angles, w = c(1, 2, 1))
}
\references{
Berens P (2009). CircStat: A MATLAB Toolbox for Circular Statistics.
Journal of Statistical Software, 31(10), 1-21.

Fisher NI (1993). Statistical Analysis of Circular Data. Cambridge University Press.
}
\seealso{
\code{\link{circ_r}}, \code{\link{circ_vtest}}
}
