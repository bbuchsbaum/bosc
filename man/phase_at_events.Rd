% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow_wrappers.R
\name{phase_at_events}
\alias{phase_at_events}
\title{Extract narrowband phase at event times}
\usage{
phase_at_events(
  events,
  dt,
  freqlim = NULL,
  fosc = NULL,
  bandwidth = 0.5,
  sd_smooth = NULL,
  leave_one_out = FALSE,
  hilbert_tol = 100,
  filtorder = 2,
  demean = TRUE,
  ...
)
}
\arguments{
\item{events}{numeric vector of event times (seconds).}

\item{dt}{time step for continuous trace (seconds).}

\item{freqlim}{length-2 numeric vector of filter band. If NULL, supply \code{fosc}.}

\item{fosc}{optional center frequency (Hz) used to derive \code{freqlim}.}

\item{bandwidth}{half-width (Hz) around \code{fosc} if \code{freqlim} is NULL.}

\item{sd_smooth}{smoothing SD for trace (seconds). If NULL and \code{fosc} provided,
defaults to \code{1/(fosc*8)}, a narrowband smoothing heuristic.}

\item{leave_one_out}{logical; if TRUE compute phases per event from traces
excluding that event.}

\item{hilbert_tol, filtorder, demean}{passed to \code{\link{narrowband_hilbert}}.}

\item{...}{further arguments passed to \code{\link{make_continuous_trace}}.}
}
\value{
numeric vector of phases (radians), same length as \code{events}.
}
\description{
Builds a continuous response trace from event times, narrowband filters it,
computes the analytic signal, and returns the instantaneous phase at each event.
Optionally computes leave-one-out phases (excluding each event from the trace).
}
