% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouped_spectrum.R
\name{group_spectrum_test_trials}
\alias{group_spectrum_test_trials}
\title{Grouped spectrum with trial-level permutation nulls}
\usage{
group_spectrum_test_trials(
  data,
  value,
  bin,
  subject,
  by = NULL,
  bins = NULL,
  fun = mean,
  na_rm = TRUE,
  complete = TRUE,
  incomplete = c("error", "drop"),
  fs,
  flim = NULL,
  null = c("shuffle_bins", "circular_shift_bins"),
  order_by = NULL,
  nrep = 1000,
  repair_incomplete = c("none", "resample"),
  max_resample = 100L,
  detrend = TRUE,
  detrend_order = 1L,
  pad_to = NULL,
  spectrum = c("amplitude", "raw_power"),
  taper = c("none", "hann", "hanning"),
  p_adjust = c("none", "fdr"),
  fcor = FALSE,
  seed = NULL
)
}
\arguments{
\item{data}{data.frame containing trial-wise values.}

\item{value}{character scalar naming the trial-level value column.}

\item{bin}{character scalar naming the bin column.}

\item{subject}{character scalar naming the subject column.}

\item{by}{optional character vector of additional grouping columns.}

\item{bins}{optional vector of target bins.}

\item{fun}{summary function used to aggregate trials within bins.}

\item{na_rm}{logical; if \code{TRUE}, remove missing trial values before
aggregation.}

\item{complete}{logical; if \code{TRUE}, request explicit rows for missing
group/bin combinations.}

\item{incomplete}{handling for incomplete observed grouped series; one of
\code{"error"} or \code{"drop"}.}

\item{fs}{sampling rate in Hz along the bin axis.}

\item{flim}{optional length-2 frequency bounds.}

\item{null}{trial-level null generator; one of \code{"shuffle_bins"} or
\code{"circular_shift_bins"}.}

\item{order_by}{optional within-group ordering column required for
\code{"circular_shift_bins"}.}

\item{nrep}{number of null draws.}

\item{repair_incomplete}{how incomplete permuted groups are handled.
\code{"none"} applies the same incomplete policy as the observed series.
\code{"resample"} redraws each group's permutation until it yields a
complete aggregated series.}

\item{max_resample}{maximum redraw attempts per group when
\code{repair_incomplete = "resample"}.}

\item{detrend}{logical; if \code{TRUE}, detrend each series before spectral
estimation.}

\item{detrend_order}{polynomial detrend order used when \code{detrend = TRUE}.}

\item{pad_to}{optional FFT length used for zero-padding each subject series
before spectral estimation.}

\item{spectrum}{spectrum scaling passed to \code{\link{spectral_peak}}.}

\item{taper}{taper passed to \code{\link{spectral_peak}}.}

\item{p_adjust}{p-value adjustment; one of \code{"none"} or \code{"fdr"}.}

\item{fcor}{logical; apply 1/f correction to each subject spectrum.}

\item{seed}{optional random seed.}
}
\value{
list with \code{observed}, \code{null}, \code{stats}, and
\code{meta}. The \code{meta} field includes the aggregated observed
grouped-series object.
}
\description{
Starts from a long trial table, aggregates within bins to form per-subject
series, and builds a null distribution by permuting bin labels within each
subject or subject-by-condition group before re-aggregating.
}
\details{
This workflow is intended for dense-sampling designs where trial timing is
varied across a regular grid and oscillatory structure is tested on the
resulting subject-by-bin series. The package implementation is generic, but
the memory-encoding application in Biba et al. (2026) is a concrete example
of this analysis pattern.
}
\references{
Biba, T. M., Decker, A., Herrmann, B., Fukuda, K., Katz, C. N.,
Valiante, T. A., & Duncan, K. (2026). Episodic memory encoding
fluctuates at a theta rhythm of 3-10 Hz. \emph{Nature Human Behaviour}.
\doi{10.1038/s41562-026-02416-5}
}
\keyword{internal}
