% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouped_spectrum.R
\name{group_spectrum_test}
\alias{group_spectrum_test}
\title{Grouped spectrum with resampled null testing}
\usage{
group_spectrum_test(
  x,
  fs,
  flim = NULL,
  null = c("shuffle_labels", "circular_shift"),
  nrep = 1000,
  detrend = TRUE,
  detrend_order = 1L,
  pad_to = NULL,
  spectrum = c("amplitude", "raw_power"),
  taper = c("none", "hann", "hanning"),
  p_adjust = c("none", "fdr"),
  fcor = FALSE,
  seed = NULL
)
}
\arguments{
\item{x}{numeric matrix with shape \code{subjects x bins}, or a grouped-series
object.}

\item{fs}{sampling rate in Hz along the bin axis.}

\item{flim}{optional length-2 frequency bounds.}

\item{null}{null generator; one of \code{"shuffle_labels"} or
\code{"circular_shift"}.}

\item{nrep}{number of null draws.}

\item{detrend}{logical; if \code{TRUE}, remove a linear trend from each
subject series before spectral estimation.}

\item{detrend_order}{non-negative integer polynomial order used when
\code{detrend = TRUE}.}

\item{pad_to}{optional FFT length used for zero-padding each subject series
before spectral estimation.}

\item{spectrum}{spectrum scaling passed to \code{\link{spectral_peak}}.}

\item{taper}{taper passed to \code{\link{spectral_peak}}.}

\item{p_adjust}{p-value adjustment; one of \code{"none"} or \code{"fdr"}.}

\item{fcor}{logical; apply 1/f correction to each subject spectrum.}

\item{seed}{optional random seed for reproducible null draws.}
}
\value{
list with \code{observed}, \code{null}, \code{stats}, and \code{meta}.
}
\description{
Builds a null distribution for the group-average spectrum by resampling each
subject series, then computes empirical p-values, z-scores, and optional FDR
correction across frequencies.
}
