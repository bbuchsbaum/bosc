% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow_wrappers.R
\name{oscillation_score_z}
\alias{oscillation_score_z}
\alias{oscore_z}
\title{Oscillation score with surrogate log-Z}
\usage{
oscillation_score_z(
  signal,
  fs,
  flim,
  nrep = 500,
  alpha = 0.05,
  keep_trend = FALSE,
  trend_dist = c("gamma"),
  trend_ddt = NULL,
  trend_alpha = 0.05,
  surrogate_method = c("auto", "event_shuffle", "event_trend", "phase_randomized"),
  signal_mode = c("auto", "event", "continuous"),
  fcor = FALSE,
  taper = c("none", "hann", "hanning"),
  ci_nboot = 0,
  ci_level = 0.95,
  tidy = FALSE,
  ...
)

oscore_z(...)
}
\arguments{
\item{signal}{numeric vector (binary or continuous).}

\item{fs}{sampling rate in Hz.}

\item{flim}{length-2 numeric vector \code{c(fmin, fmax)} in Hz.}

\item{nrep}{number of surrogates.}

\item{alpha}{significance level for one-tailed Z-thresholding.}

\item{keep_trend, trend_dist, trend_ddt, trend_alpha}{passed to surrogates.}

\item{surrogate_method, signal_mode}{passed to \code{\link{oscillation_score_surrogates}}.}

\item{fcor}{logical; apply 1/f correction.}

\item{taper}{taper before FFT.}

\item{ci_nboot}{number of bootstrap draws for the log-Z confidence interval.
Set to 0 to skip CI estimation.}

\item{ci_level}{confidence level for bootstrap CI.}

\item{tidy}{logical; if TRUE return a one-row data.frame.}

\item{...}{further arguments passed to \code{\link{oscillation_score}}.}
}
\value{
list (default) or data.frame if \code{tidy=TRUE}.
}
\description{
Computes the oscillation score, generates a surrogate distribution, and returns
the log-Z score:
\deqn{Z = (log(O) - mean(log(O_{rep}))) / sd(log(O_{rep}))}.
}
