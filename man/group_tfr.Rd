% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouped_phase_tfr.R
\name{group_tfr}
\alias{group_tfr}
\title{Grouped time-frequency map via narrowband Hilbert decomposition}
\usage{
group_tfr(
  x,
  fs,
  freqs,
  bandwidth = NULL,
  detrend = TRUE,
  reflect = FALSE,
  edge = 0,
  filtorder = 2,
  demean = TRUE,
  tol = 100
)
}
\arguments{
\item{x}{numeric matrix with shape \code{subjects x bins}, or a grouped-series
object.}

\item{fs}{sampling rate in Hz along the bin axis.}

\item{freqs}{numeric vector of center frequencies.}

\item{bandwidth}{optional half-bandwidth in Hz. If \code{NULL}, a default is
derived from \code{freqs}.}

\item{detrend}{logical; if \code{TRUE}, detrend each subject series first.}

\item{reflect}{logical; if \code{TRUE}, reflect the series at both ends before
filtering and trim back to the original span.}

\item{edge}{number of bins to trim from each edge after filtering.}

\item{filtorder, demean, tol}{passed to \code{\link{narrowband_hilbert}}.}
}
\value{
list with \code{observed} and \code{meta}.
}
\description{
Computes a grouped time-frequency amplitude map by filtering each subject
series at a sequence of center frequencies and averaging the resulting
narrowband amplitudes.
}
