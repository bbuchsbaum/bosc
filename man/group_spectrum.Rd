% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouped_spectrum.R
\name{group_spectrum}
\alias{group_spectrum}
\title{Grouped spectrum for binned subject-level series}
\usage{
group_spectrum(
  x,
  fs,
  flim = NULL,
  detrend = TRUE,
  detrend_order = 1L,
  pad_to = NULL,
  spectrum = c("amplitude", "raw_power"),
  taper = c("none", "hann", "hanning"),
  average = TRUE,
  fcor = FALSE
)
}
\arguments{
\item{x}{numeric matrix with shape \code{subjects x bins}, or a list with
fields \code{data}, \code{subjects}, and \code{bins}.}

\item{fs}{sampling rate in Hz along the bin axis.}

\item{flim}{optional length-2 frequency bounds.}

\item{detrend}{logical; if \code{TRUE}, remove a linear trend from each
subject series before spectral estimation.}

\item{detrend_order}{non-negative integer polynomial order used when
\code{detrend = TRUE}. The default \code{1} removes a linear trend, while
higher orders remove higher-order polynomial structure.}

\item{pad_to}{optional FFT length used for zero-padding each subject series
before spectral estimation.}

\item{spectrum}{spectrum scaling passed to \code{\link{spectral_peak}}.}

\item{taper}{taper passed to \code{\link{spectral_peak}}.}

\item{average}{logical; if \code{TRUE}, include the group-average spectrum.}

\item{fcor}{logical; apply 1/f correction to each subject spectrum.}
}
\value{
list with \code{observed} and \code{meta} fields.
}
\description{
Computes per-subject spectra and their group average for a regular matrix of
subject-by-bin values or a grouped-series object.
}
